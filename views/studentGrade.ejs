<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
  </head>

  <body>
    <h4>your grade is</h4>
    <% if(studentGrade >= 90){ %>
    <p>you are excellent A</p>
    <%}else if(studentGrade >= 80 && studentGrade < 90 ){ %>
    <p>you are very good B</p>
    <%}else if(studentGrade >= 70 && studentGrade < 80 ) {%>
    <p>you are good C</p>
    <%}else if(studentGrade>= 60 && studentGrade < 70 ) {%>
    <p>you pass D</p>
    <% } else if(studentGrade>= 50 && studentGrade < 60 ) {%>
    <p>you faild F</p>
    <% }%>
  </body>
</html>
